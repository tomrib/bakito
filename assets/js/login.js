const boxuser = document.getElementById('boxuser');
const loginContainer = document.getElementById('loginContainer');
boxuser.addEventListener('click', function() {
    loginContainer.classList.toggle('rowuser')
})